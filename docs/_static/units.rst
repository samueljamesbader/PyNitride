.. _units:

=====================================
Units in PyNitride
=====================================


, so no unit conversion anywhere else in the code is necessary, except when formatting results to print for a user.  In that case, one should use the :py:func:`~pynitride.paramdb.to_unit` function to convert to the desired units, so again, the internal units system is a black box.
