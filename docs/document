#!/bin/bash
if [[ "$PWD" = *docs ]]; then
    echo "Run this script from main Poissolve directory"
    exit
fi
cd docs
sphinx-apidoc -o . ../ ../examples ../poissolve/tests ../setup.py -f -e
make html
